image:
  size: 250
  minisize: 100
user:
  email:
    max_length: 56
    regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  password:
    min_length: 6
order:
  address:
    regex: !ruby/regexp /\(?([0-9]{3})\)?([ .-]?)([0-9]{3})\2([0-9]{4})/
sort:
  ascc: "asc"
  descc: "desc"
