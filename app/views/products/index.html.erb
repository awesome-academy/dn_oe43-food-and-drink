<%= render "products/filter_form" %>
<div>
  <% if @products.empty? %>
    <h1><%= t "filter.not_found" %></h1>
  <% else %>
    <ul>
      <%= render @products %>
    </ul>
  <% end %>
</div>
<br>
<p><%= will_paginate @products %></p>
